spring.application.name=finance-manager

# PostgreSQL Database Configuration
# Values are read from environment variables (set on Render or locally in .env)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/finance_manager}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

# Automatically create tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Prevent circular references in Jackson
spring.jackson.serialization.fail-on-empty-beans=false
